# সরকারি চাকরি প্রস্তুতি (Govt Job Prep App)

React + Supabase দিয়ে বানানো একটা প্রাইভেট প্র্যাকটিস অ্যাপ — Gmail দিয়ে লগইন,
বিষয়ভিত্তিক/টপিকভিত্তিক MCQ প্র্যাকটিস, পূর্ববর্তী বছরের প্রশ্নপত্র, এবং
প্রতিটা ইউজারের নিজস্ব প্রাইভেট প্রোগ্রেস ট্র্যাকিং।

**ডেটা প্রাইভেসি:** Supabase-এর Row Level Security (RLS) ব্যবহার করে নিশ্চিত করা
হয়েছে যে প্রতিটা ইউজার শুধু নিজের প্র্যাকটিস হিস্টোরি দেখতে/লিখতে পারবে। কোনো
ইউজারের ডেটা আরেকজনের একাউন্টে কখনো দেখা যাবে না — এটা কোড-লেভেল ফিল্টার না,
ডেটাবেজ-লেভেল নিয়ম (`schema.sql` ফাইলের RLS policies দেখুন), তাই বাইপাস করা যায় না।

সম্পূর্ণ ফ্রি টায়ারেই চলবে: Supabase ফ্রি প্ল্যান + Vercel/Netlify ফ্রি হোস্টিং।

---

## ধাপ ১: Supabase প্রজেক্ট বানান (ফ্রি, কার্ড লাগে না)

1. https://supabase.com এ গিয়ে সাইন আপ করুন
2. "New Project" ক্লিক করুন — একটা নাম দিন (যেমন `govtjob-prep`), একটা ডাটাবেজ পাসওয়ার্ড সেট করুন, রিজিয়ন হিসেবে **Singapore (ap-southeast-1)** বেছে নিন (বাংলাদেশ থেকে সবচেয়ে কাছে)
3. প্রজেক্ট তৈরি হতে ১-২ মিনিট লাগবে

## ডাটাবেজ ক্লিন করে আবার শুরু করা (দরকার হলে)

যদি নিজে Supabase-এ কোনো SQL চালিয়ে থাকেন এবং সেগুলো রাখতে না চান, বা কোনটা কী
বদলেছে ভুলে গিয়ে থাকেন — নিরাপদে পুরো ডাটাবেজ ক্লিন করে আবার শুরু করা যায়:

1. Supabase Dashboard → **SQL Editor** → **New query**
2. এই রিপোর **`reset_database.sql`** ফাইলের কনটেন্ট পেস্ট করে **Run** করুন (এটা শুধু
   এই অ্যাপের নিজের বানানো টেবিল/ফাংশন মুছবে — Supabase-এর নিজস্ব auth সিস্টেম বা
   প্রজেক্টের অন্য কিছুতে হাত দেবে না)
3. তারপর **`schema.sql`**-এর পুরো কনটেন্ট আরেকটা নতুন query-তে পেস্ট করে **Run** করুন

**ভালো খবর:** `schema.sql` এখন এতদিনের সব ফিচারের migration একসাথে মিশিয়ে দেওয়া
(Admin, MCQ+Short Answer, Topic Tree, কুইক সার্চ, ফেভারিট, কুইজ বিল্ডার, লাইভ পরীক্ষা —
সব)। তাই রিসেটের পর **এই একটা ফাইল একবার চালালেই** পুরো ডাটাবেজ সম্পূর্ণ আপ-টু-ডেট
অবস্থায় চলে আসবে — আলাদা আলাদা migration ফাইল খুঁজে ক্রমানুসারে চালানোর দরকার নেই।

⚠️ পুরো Supabase প্রজেক্ট ডিলিট করে নতুন বানাবেন না — তাহলে Vercel-এর
`VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` আর Google login-এর callback URL
আবার নতুন করে সেট করা লাগবে। উপরের পদ্ধতিতে প্রজেক্ট একই থাকে, শুধু ভেতরের ডেটা
রিসেট হয় — তাই Vercel/Google-এর কোনো সেটিংস আবার করতে হবে না। তবে **রিসেট করলে
আপনার নিজের একাউন্টও নতুন করে Admin বানাতে হবে** ("নিজেকে Admin বানানো" অংশ আবার
করুন) এবং sample প্রশ্ন ছাড়া বাকি সব প্রশ্ন/সাবজেক্ট মুছে যাবে।

## ধাপ ২: ডাটাবেজ টেবিল বানান

1. Supabase Dashboard-এ বাম পাশের মেনু থেকে **SQL Editor** এ যান
2. **New query** ক্লিক করুন
3. এই রিপোর দিয়ে আসা `schema.sql` ফাইলের **পুরো কনটেন্ট** কপি করে পেস্ট করুন
4. **Run** ক্লিক করুন — এতে সব টেবিল, প্রাইভেসি নিয়ম (RLS), এবং কিছু নমুনা প্রশ্ন তৈরি হয়ে যাবে

## ধাপ ৩: Google লগইন চালু করুন

1. Supabase Dashboard-এ **Authentication → Providers** এ যান
2. **Google** খুঁজে বের করে Enable করুন
3. Google Cloud Console (https://console.cloud.google.com) এ গিয়ে একটা OAuth Client ID বানাতে হবে:
   - **APIs & Services → Credentials → Create Credentials → OAuth client ID**
   - Application type: **Web application**
   - Authorized redirect URIs-এ Supabase-এর দেওয়া callback URL বসান (Supabase-এর Google provider পেজেই এটা দেখানো থাকবে, যেমন `https://xxxxx.supabase.co/auth/v1/callback`)
4. Google থেকে পাওয়া **Client ID** ও **Client Secret** Supabase-এর Google provider সেটিংসে বসিয়ে Save করুন

## ধাপ ৪: প্রজেক্টের Key কপি করুন

Supabase Dashboard-এ **Project Settings → API** এ যান, সেখান থেকে কপি করুন:
- **Project URL**
- **anon public** key

## ধাপ ৫: লোকালি চালান

```bash
npm install
cp .env.example .env
```

`.env` ফাইলটা খুলে ধাপ ৪ থেকে পাওয়া মান বসান:
```
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJxxxxxxxxxxxxxxxx
```

তারপর:
```bash
npm run dev
```

ব্রাউজারে `http://localhost:5173` খুলুন।

## ধাপ ৬: ফ্রি হোস্টিং-এ ডিপ্লয় করুন

**Vercel** (সবচেয়ে সহজ):
1. এই কোড GitHub-এ পুশ করুন
2. https://vercel.com এ গিয়ে GitHub রিপো import করুন
3. Environment Variables-এ `VITE_SUPABASE_URL` ও `VITE_SUPABASE_ANON_KEY` যোগ করুন
4. Deploy করুন

Deploy হওয়ার পর যে URL পাবেন (যেমন `https://your-app.vercel.app`), সেটা Supabase-এর
Authentication → URL Configuration-এ **Site URL** ও **Redirect URLs**-এ যোগ করে দিন,
নাহলে লাইভ সাইটে Google লগইন কাজ করবে না। একই কাজ Netlify দিয়েও করা যায়।

## নিজেকে Admin বানানো (Admin Panel ব্যবহার করতে)

Admin Panel এখন মূল অ্যাপের ভেতরে কোনো ট্যাব হিসেবে না — বরং **আলাদা URL**-এ থাকে:

```
https://your-app.vercel.app/admin
```

সাধারণ ইউজাররা এই লিংক জানলেও ভেতরে ঢুকতে পারবে না — Admin না হলে "এই পাতা শুধু
Admin-দের জন্য" মেসেজ দেখাবে। Subject, Topic, Exam (পূর্ববর্তী বছরের প্রশ্নপত্র),
Question যোগ/মুছা এবং প্রশ্ন **JSON দিয়ে bulk upload** — সবকিছু এখান থেকেই হয়।

**নিজেকে Admin বানানোর ধাপ:**
1. প্রথমে আপনার লাইভ সাইটে গিয়ে Gmail দিয়ে অন্তত একবার লগইন করুন (এতে আপনার প্রোফাইল রো তৈরি হবে)
2. Supabase Dashboard → **SQL Editor** → **New query**
3. এই রিপোর `admin_migration.sql` ফাইলের **পুরো কনটেন্ট** কপি-পেস্ট করে **Run** করুন
   (এটা `is_admin` কলাম ও Admin-only write নিয়ম তৈরি করবে)
4. ফাইলের একদম নিচে এই লাইনটা খেয়াল করুন:
   ```sql
   -- update public.profiles set is_admin = true where email = 'your-email@gmail.com';
   ```
   এটা **নতুন query** হিসেবে আলাদা করে চালান — শুরুর `--` কমেন্ট চিহ্ন সরিয়ে দিয়ে,
   আর `your-email@gmail.com` জায়গায় আপনার নিজের Gmail বসিয়ে **Run** করুন
5. এখন `https://your-app.vercel.app/admin` এ যান, Gmail দিয়ে লগইন করুন (যদি আগে থেকে
   লগইন না থাকে) — Admin Panel খুলে যাবে

⚠️ যাকে Admin বানাবেন সে Subject/Topic/Exam/Question যোগ-মুছা করতে পারবে — এটা শুধু
বিশ্বস্ত কাউকে (যেমন নিজেকে) দিন। এই Admin ক্ষমতা দিয়ে অন্য কারো ব্যক্তিগত
প্র্যাকটিস ডেটা/স্কোর দেখা যায় না — শুধু প্রশ্ন ব্যাংক এডিট করা যায়।

**টেকনিক্যাল নোট:** `/admin` একটা React route (client-side), সার্ভারে আলাদা কোনো
ফাইল/ফোল্ডার না। `vercel.json`-এ একটা rewrite rule দেওয়া আছে যাতে সরাসরি
`/admin` লিংকে গেলে বা রিফ্রেশ করলেও পেজ ঠিকভাবে লোড হয় — এটা এমনিতেই কাজ করবে,
আলাদা কিছু সেটাপ করতে হবে না।

## ফিচার আর্কিটেকচার — প্লাগ-ইন স্টাইল (আপডেট হয়েছে)

কোড রিফ্যাক্টর করা হয়েছে যাতে প্রতিটা ফিচার সত্যিকারের "প্লাগ-ইন"-এর মতো কাজ করে —
কোনো ফিচার যোগ/মুছা/বদলালে **মূল অ্যাপের কোনো ফাইলে হাত দিতে হয় না**।

**কীভাবে কাজ করে:**
- `src/lib/AppContext.jsx` — একটা shared "context" যেখান থেকে যেকোনো ফিচার
  কম্পোনেন্ট নিজে থেকেই `useApp()` কল করে দরকারি সব কিছু (user, subjects,
  topics, exams, কুইজ শুরু করার ফাংশন ইত্যাদি) নিয়ে নিতে পারে — কোনো prop
  পাস করার দরকার নেই।
- `src/features/registry.js` — Student পোর্টালের সব ফিচারের তালিকা (কোন
  কম্পোনেন্ট, কোন নাম, কোন ক্রমে ন্যাভবারে দেখাবে)। **এই একটা ফাইলই মূল অ্যাপের
  একমাত্র জায়গা যেটা কোনো ফিচার যোগ/মুছার সময় বদলাতে হয়** — `MainApp.jsx` ও
  `TopBar.jsx` এই লিস্ট থেকে automatically ন্যাভবার ও রাউটিং বানিয়ে নেয়।
- `src/features/adminRegistry.js` — একই প্যাটার্নে Admin Panel-এর ট্যাবগুলোর
  তালিকা (`src/components/admin/` ফোল্ডারে প্রতিটা ট্যাব আলাদা ফাইল)।

**কোনো ফিচার বন্ধ/মুছে ফেলতে চাইলে:**
`src/features/registry.js` (বা admin ফিচারের জন্য `adminRegistry.js`) ফাইলে
গিয়ে সেই ফিচারের এন্ট্রি (এক লাইন) ও তার import লাইন মুছে দিন। ব্যস — বাকি অ্যাপ
এতে কোনোভাবে প্রভাবিত হবে না, কারণ ফিচারটার কম্পোনেন্ট ফাইলও সম্পূর্ণ স্বনির্ভর
(self-contained), অন্য কোনো ফাইল সেটার ওপর নির্ভর করে না।

**নতুন ফিচার যোগ করতে চাইলে:**
`src/components/` এ একটা নতুন কম্পোনেন্ট বানান যেটা `useApp()` দিয়ে নিজের দরকারি
ডেটা নিয়ে নেয়, তারপর `registry.js`-এ একটা লাইন যোগ করুন — কোনো prop-wiring বা
`MainApp.jsx`/`TopBar.jsx` এডিট করার দরকার নেই।

নতুন কোনো migration লাগবে না — এটা সম্পূর্ণ কোড-স্ট্রাকচার রিফ্যাক্টর, ডাটাবেজ অপরিবর্তিত।

## লাইভ পরীক্ষা (নতুন ফিচার)

**🔴 লাইভ পরীক্ষা** — নির্দিষ্ট একটা সময়ে সবাই একসাথে একই পরীক্ষা দিতে পারবে, শেষে
**নাম ও স্কোর সহ পূর্ণ র‍্যাংকিং** দেখা যাবে — যাতে সবাই একে অপরকে চিনতে পারে ও
প্রতিযোগিতার আবহ তৈরি হয়।

**⚠️ প্রাইভেসি নোট (গুরুত্বপূর্ণ):** এটা একটা **সচেতন ব্যতিক্রম, শুধু Live Exam
লিডারবোর্ডের জন্য**। বাকি পুরো অ্যাপ আগের মতোই প্রাইভেট থাকে — নিজের প্র্যাকটিস
হিস্টোরি, ড্যাশবোর্ড স্ট্যাটস, ফেভারিট, টপিক প্রোগ্রেস — এসবের কোনোটাই অন্য কেউ
দেখতে পারে না, শুধু Live Exam-এ অংশ নিলে সেই নির্দিষ্ট পরীক্ষার লিডারবোর্ডে আপনার
নাম ও স্কোর সবাই দেখতে পাবে (এবং আপনিও অন্যদেরটা দেখতে পাবেন)। এই এক্সপোজার একদম
নির্দিষ্ট — শুধু যারা সেই নির্দিষ্ট Live Exam সম্পন্ন করেছে তাদের নাম+স্কোরই দেখা
যায়, পুরো ইউজার লিস্ট বা অন্য কোনো ডেটা না।

**Admin থেকে লাইভ পরীক্ষা তৈরি:** `/admin` → "🔴 লাইভ পরীক্ষা" ট্যাবে শিরোনাম, শুরুর
সময়, সময়সীমা, প্রশ্ন সংখ্যা দিয়ে তৈরি করুন। প্রশ্ন হয় কোনো একটা বিদ্যমান Exam Archive
এন্ট্রি থেকে আসবে, অথবা একটা নির্দিষ্ট বিষয় থেকে র‍্যান্ডম বেছে নেওয়া হবে (দুটোর কোনোটাই
না দিলে পুরো প্রশ্নভান্ডার থেকে আসবে)।

**Student পোর্টালে:** নির্ধারিত সময় হওয়ার আগে "অপেক্ষা করুন" দেখাবে, চলাকালীন
"পরীক্ষায় যোগ দিন" বাটন সক্রিয় হবে (কেউ দেরিতে যোগ দিলে তার সময় স্বয়ংক্রিয়ভাবে কমে
যাবে, যাতে সবার শেষ হওয়ার সময় এক থাকে), আর শেষ হলে বা অংশ নেওয়ার পর "🏅 র‍্যাংকিং দেখুন"।

**চালু করতে migration লাগবে (দুটো, ক্রমানুসারে):**
1. Supabase Dashboard → **SQL Editor** → **New query**
2. প্রথমে **`live_exam_migration.sql`** চালান (মূল টেবিল ও ফাংশন তৈরি করবে)
3. তারপর **`live_exam_names_migration.sql`** চালান (লিডারবোর্ডে নাম দেখানোর জন্য
   ফাংশনটা আপডেট করবে)

যদি আগেই শুধু প্রথমটা চালিয়ে থাকেন (anonymous ভার্সন), তাহলে এখন শুধু দ্বিতীয়টা
চালালেই যথেষ্ট।

## ভুল ও বাদ পড়া প্রশ্ন রিভিউ (নতুন ফিচার)

কুইজ/পরীক্ষা শেষে এখন **"এড়িয়ে যান"** বাটন দিয়ে কোনো প্রশ্ন স্কিপ করা যায় (আগে
প্রতিটা প্রশ্নের উত্তর দিতেই হতো)। কুইজ শেষে ফলাফল স্ক্রিনে যদি কোনো প্রশ্ন ভুল হয়
বা এড়িয়ে যাওয়া হয়, তাহলে **"❌ ভুল ও বাদ পড়া প্রশ্ন রিভিউ করুন"** বাটন দেখা যাবে —
এতে ক্লিক করলে শুধু সেই নির্দিষ্ট সেশনের ভুল/বাদ-পড়া প্রশ্নগুলোই (সঠিক উত্তর ও ব্যাখ্যাসহ)
আলাদাভাবে রিভিশন করা যায়, পুরো প্রশ্নপত্র আবার স্ক্রল করতে হয় না।

নতুন কোনো migration লাগবে না — শুধু কোড ফাইল re-upload করলেই হবে:
`src/components/Quiz.jsx`, `src/components/Result.jsx`, `src/MainApp.jsx`,
`src/styles.css`, আর নতুন ফাইল `src/components/SessionReview.jsx`।

(Live MCQ-এর "Wrong & Unanswered" ফিচারের সমতুল্য।)

## কুইজ বিল্ডার (নতুন ফিচার)

**🎛️ কুইজ বিল্ডার** ট্যাবে নিজের মতো কাস্টমাইজ করে কুইজ বানানো যায় — ৪টা অপশন দিয়ে:

1. **প্রশ্নের উৎস** — সব প্রশ্ন থেকে র‍্যান্ডম / ফেভারিট প্রশ্ন / ভুল করা প্রশ্ন / উত্তর না
   দেওয়া প্রশ্ন / সঠিক করা প্রশ্ন / শুধু পরীক্ষার প্রশ্ন (এগুলো ব্যবহারকারীর নিজের
   প্র্যাকটিস হিস্টোরি থেকে হিসাব হয় — প্রাইভেট)
2. **বিষয়** — একাধিক বিষয় বেছে নেওয়া যায়, কিছু না বেছে নিলে সব বিষয় মিলিয়ে আসবে
3. **প্রশ্ন সংখ্যা** — নিজের ইচ্ছামতো সংখ্যা
4. **সময়সীমা (মিনিট)** — দিলে কুইজে কাউন্টডাউন টাইমার চালু হয়ে যাবে, সময় শেষ হলে
   অটোমেটিক্যালি ফলাফল দেখাবে

**চালু করতে migration লাগবে:**
1. Supabase Dashboard → **SQL Editor** → **New query**
2. এই রিপোর **`quiz_builder_migration.sql`** ফাইলের কনটেন্ট পেস্ট করে **Run** করুন
   (এটা practice_sessions টেবিলে 'custom' মোড অনুমোদন করবে)

(Live MCQ-এর "Quiz Master" ফিচারের সমতুল্য — নাম আলাদা রাখা হয়েছে।)

## জব সল্যুশন [টপিকভিত্তিক] — টপিক ট্রি-র সাথে একীভূত করা হয়েছে

Live MCQ-এর "জব সল্যুশন [টপিকভিত্তিক]" ফিচারটা কার্যত "টপিক গুরু"-রই একটা সাব-সেট
(টপিক ধরে বিগত বছরের প্রশ্ন + Unique ফিল্টার + ব্যাখ্যা + Random Quiz + প্রোগ্রেস) —
তাই আলাদা ডুপ্লিকেট পেজ না বানিয়ে **🧭 টপিক ট্রি**-তেই যোগ করা হয়েছে:

- **ফেভারিট বাটন ঠিক করা হয়েছে** — আগে টপিক ট্রি ও পরীক্ষা আর্কাইভের প্রশ্ন-প্রিভিউতে ★ বাটন
  থাকলেও কাজ করত না (কোনো কাজ হতো না ক্লিক করলে); এখন সত্যিকারের ফেভারিট/আনফেভারিট করে
- **"শুধু পরীক্ষায় আসা প্রশ্ন"** ফিল্টার — টপিক পড়ার সময় চাইলে শুধু আসল পরীক্ষায় আসা
  প্রশ্নগুলোই দেখা যাবে (সাধারণ প্র্যাকটিস প্রশ্ন বাদ দিয়ে)
- **প্রশ্নের ধরন ফিল্টার** (সব / শুধু MCQ / শুধু Short Answer)

নতুন কোনো migration লাগবে না — শুধু `src/components/TopicGuru.jsx` ও
`src/components/ExamsList.jsx` ফাইল re-upload করলেই হবে।

## পরীক্ষা আর্কাইভ (আপডেট হয়েছে)

আগের "প্রশ্নপত্র" ট্যাবটা এখন আরও সমৃদ্ধ — **📁 পরীক্ষা আর্কাইভ**:

- পরীক্ষার নাম বা প্রতিষ্ঠান দিয়ে সার্চ
- প্রতিষ্ঠান দিয়ে ফিল্টার, ও "শুধু যেগুলো এখনো দেওয়া হয়নি" টগল
- প্রতিটা পরীক্ষায় **✓ দেওয়া হয়েছে / দেওয়া হয়নি** ট্যাগ (নিজের প্র্যাকটিস হিস্টোরি থেকে,
  প্রাইভেট — RLS দিয়ে সুরক্ষিত)
- **"প্রশ্ন দেখুন"** বাটন — কুইজ শুরু করার আগে পুরো প্রশ্নপত্রের প্রশ্ন-উত্তর-ব্যাখ্যা প্রিভিউ
  করা যায় (কোনো সেশন/স্কোর রেকর্ড হয় না, শুধু দেখা)

এটার জন্য নতুন কোনো migration লাগবে না — আগের টেবিল/RLS দিয়েই কাজ করে। শুধু
`src/components/ExamsList.jsx`, `src/MainApp.jsx`, `src/components/TopBar.jsx`,
`src/styles.css` ফাইল re-upload করলেই হবে।

## টপিক ট্রি (নতুন ফিচার)

**🧭 টপিক ট্রি** ট্যাবে এখন বিষয়ের ভেতরে নেস্টেড টপিক/সাব-টপিক ট্রি ধরে গভীরে গিয়ে
পড়াশোনা করা যায় — যেমন "বাংলা সাহিত্য" → "আধুনিক যুগ" → "রবীন্দ্রনাথ ঠাকুর"। যেকোনো
পর্যায়ে "এই টপিকের সব প্রশ্ন পড়ুন" চাপলে সেই টপিক ও তার সব সাব-টপিকের প্রশ্ন-উত্তর-ব্যাখ্যা
একসাথে দেখা যায় (কুইজ না, সরাসরি পড়ার মোড)। আরও আছে:

- **Unique ফিল্টার** — একই প্রশ্ন একাধিকবার থাকলে ডুপ্লিকেট বাদ দিয়ে শুধু ইউনিক প্রশ্ন দেখায়
- **প্রোগ্রেস ট্র্যাকিং** — কোন টপিক পড়া হয়ে গেছে সেটা ✓ চিহ্ন দিয়ে মনে রাখে (প্রাইভেট, শুধু নিজের)
- **Random Quiz বাটন** — পড়া শেষে সরাসরি সেই টপিক থেকে কুইজ দেওয়া যায়

**Admin Panel থেকে সাব-টপিক বানানো:** `/admin` → "টপিক" ট্যাবে টপিক যোগ করার সময়
"প্যারেন্ট টপিক" ড্রপডাউনে আরেকটা টপিক বেছে নিলে সেটা তার সাব-টপিক হয়ে যাবে। প্যারেন্ট
ছাড়া যোগ করলে সেটা মূল টপিক থাকবে। যত ইচ্ছা গভীরে নেস্ট করা যাবে।

**চালু করতে migration লাগবে:**
1. Supabase Dashboard → **SQL Editor** → **New query**
2. এই রিপোর **`topic_guru_migration.sql`** ফাইলের পুরো কনটেন্ট পেস্ট করে **Run** করুন

এটা টপিকে `parent_topic_id` কলাম (নেস্টিং-এর জন্য) ও `topic_reads` টেবিল (প্রোগ্রেস
ট্র্যাকিং, প্রাইভেট) তৈরি করবে।

## কুইক সার্চ ও ফেভারিট (নতুন ফিচার)

Student পোর্টালে এখন দুটো নতুন ট্যাব আছে:

- **🔍 সার্চ** — যেকোনো শব্দ, টপিক বা বিষয় লিখলে সব প্রশ্নের মধ্যে থেকে প্রাসঙ্গিকগুলো
  খুঁজে দেখায় (ভুল বানানেও কাজ করে — trigram-ভিত্তিক fuzzy match ব্যবহার করে)। প্রতিটা
  ফলাফলে প্রশ্ন, সঠিক উত্তর, ব্যাখ্যা, এবং বিষয়/টপিক/exam ট্যাগ দেখা যায়।
- **⭐ ফেভারিট** — সার্চ রেজাল্টে ★ চিহ্নে ক্লিক করে যেকোনো প্রশ্ন ফেভারিট/বুকমার্ক করা
  যায়, পরে রিভিশনের জন্য এই ট্যাব থেকে দেখা যাবে। এটাও প্রাইভেট — একজনের ফেভারিট
  আরেকজন দেখতে পাবে না (RLS দিয়ে সুরক্ষিত)।

**চালু করতে migration লাগবে:**
1. Supabase Dashboard → **SQL Editor** → **New query**
2. এই রিপোর **`smart_search_migration.sql`** ফাইলের পুরো কনটেন্ট পেস্ট করে **Run** করুন

এটা করবে: fuzzy-search-এর জন্য `pg_trgm` extension চালু করবে, একটা সার্চ ফাংশন
(`smart_search_questions`) বানাবে, আর `favorites` টেবিল + প্রাইভেসি নিয়ম তৈরি করবে।

## প্রশ্নের তালিকা দেখা (Admin Panel)

`/admin` পেজের "প্রশ্ন" ট্যাবে এখন যোগ করা সব প্রশ্নের একটা তালিকা দেখা যায় —
সবচেয়ে নতুন প্রশ্ন উপরে, সাথে বিষয় ও প্রশ্নপত্র দিয়ে ফিল্টার করার অপশন, এবং প্রতিটা
প্রশ্নের পাশে মুছে ফেলার বাটন। এই তালিকা কাজ করার জন্য আরেকটা ছোট migration লাগবে:

1. Supabase Dashboard → **SQL Editor** → **New query**
2. এই রিপোর **`questions_list_migration.sql`** ফাইলের কনটেন্ট পেস্ট করে **Run** করুন
   (এটা `created_at` কলাম যোগ করবে যাতে তালিকা তারিখ অনুযায়ী সাজানো যায়)

## প্রশ্নের ধরন আপডেট (MCQ + Short Answer) — বিদ্যমান ডাটাবেজ থাকলে

যদি আপনি আগেই `schema.sql` ও `admin_migration.sql` চালিয়ে থাকেন, প্রশ্নে এখন দুই
ধরনের সাপোর্ট যোগ করতে একটা নতুন migration লাগবে:

1. Supabase Dashboard → **SQL Editor** → **New query**
2. এই রিপোর `questions_upgrade_migration.sql` ফাইলের পুরো কনটেন্ট কপি-পেস্ট করে **Run** করুন

এটা করবে:
- `question_type` কলাম যোগ করবে (`mcq` অথবা `short`, বিদ্যমান সব প্রশ্ন `mcq` থেকে যাবে)
- `short_answer` কলাম যোগ করবে (short-answer প্রশ্নের সঠিক উত্তর টেক্সট আকারে রাখার জন্য)
- MCQ-এর অপশন/সঠিক-উত্তর কলামগুলো আর বাধ্যতামূলক থাকবে না (short-answer প্রশ্নে এগুলো ফাঁকা থাকে)
- `difficulty` কলাম সম্পূর্ণ মুছে ফেলবে (আর ব্যবহার হয় না)

নতুনভাবে (fresh) সেটআপ করলে `schema.sql`-এই এসব থাকবে, আলাদা কিছু চালাতে হবে না।

## প্রশ্ন যোগ করা

**একটা একটা করে:** `/admin` পেজের "প্রশ্ন" ট্যাবে উপরে "প্রশ্নের ধরন" থেকে **MCQ** (৪টা
অপশন) অথবা **Short Answer** (সরাসরি টেক্সট উত্তর, যেমন "বাংলাদেশের রাজধানীর নাম কী?"
→ "ঢাকা") বেছে নিয়ে ফর্ম পূরণ করুন।

**একসাথে অনেকগুলো (JSON Bulk Upload):** `/admin` পেজের "প্রশ্ন" ট্যাবের নিচে একটা
textarea আছে, সেখানে এই ফরম্যাটে JSON বসিয়ে "JSON আপলোড করুন" চাপুন:

```json
[
  {
    "question_type": "mcq",
    "subject_slug": "bangla",
    "topic_name_en": "Grammar",
    "exam_slug": "",
    "question_text": "প্রশ্নটি এখানে লিখুন",
    "option_a": "ক",
    "option_b": "খ",
    "option_c": "গ",
    "option_d": "ঘ",
    "correct_option": "a",
    "explanation": "ব্যাখ্যা (ঐচ্ছিক)"
  },
  {
    "question_type": "short",
    "subject_slug": "gk",
    "topic_name_en": "Bangladesh Affairs",
    "exam_slug": "",
    "question_text": "বাংলাদেশের রাজধানীর নাম কী?",
    "short_answer": "ঢাকা",
    "explanation": ""
  }
]
```

- `question_type` — `mcq` অথবা `short`; না দিলে `mcq` ধরে নেওয়া হবে
- `subject_slug` — আবশ্যক, Admin Panel-এর "বিষয়" ট্যাবে যে slug দিয়েছেন সেটাই বসাতে হবে
- `topic_name_en` — ঐচ্ছিক, না দিলে টপিক ছাড়াই প্রশ্নটা যোগ হবে
- `exam_slug` — ঐচ্ছিক, দিলে প্রশ্নটা সেই প্রশ্নপত্রের অংশ হিসেবে গণ্য হবে
- MCQ হলে: `option_a`–`option_d` ও `correct_option` (`a`/`b`/`c`/`d`) আবশ্যক
- Short Answer হলে: `short_answer` আবশ্যক (এটাই সঠিক উত্তর হিসেবে মিলিয়ে দেখা হবে)

আপলোডের পর কোনো প্রশ্ন বাদ পড়লে (যেমন ভুল `subject_slug`) সেটার কারণ মেসেজে দেখানো হবে।

## পূর্ববর্তী বছরের প্রশ্নপত্র (Exam) Bulk Upload

শুধু প্রশ্ন না, **Exam-ও এখন JSON দিয়ে একসাথে অনেকগুলো যোগ করা যায়** — `/admin`
পেজের "প্রশ্নপত্র" ট্যাবের নিচে:

```json
[
  { "name": "৪৪তম বিসিএস প্রিলিমিনারি", "organization": "BPSC", "year": 2024, "slug": "bcs-44-preli" },
  { "name": "সহকারী শিক্ষক নিয়োগ পরীক্ষা", "organization": "DPE", "year": 2023, "slug": "primary-teacher-2023" }
]
```

একই `slug` আগে থেকে থাকলে সেটার তথ্য আপডেট হয়ে যাবে (ডুপ্লিকেট তৈরি হবে না)। এরপর
প্রশ্ন bulk upload করার সময় সংশ্লিষ্ট `exam_slug` ব্যবহার করে প্রশ্নগুলোকে এই
প্রশ্নপত্রের সাথে যুক্ত করতে পারবেন।

## ভবিষ্যতে নতুন ফিচার যোগ করা

কোনো নতুন ফিচার লাগলে (নতুন পেজ, ডিজাইন পরিবর্তন, নতুন কুইজ মোড, ইত্যাদি) — Claude-কে
বললেই কোড লিখে দেবে। এরপর:
1. যেসব ফাইল বদলেছে সেগুলো GitHub-এ পুরনোটার জায়গায় নতুন করে আপলোড করুন (Add file → Upload files, একই নামে থাকলে GitHub নিজে থেকেই ওভাররাইট করবে)
2. Vercel স্বয়ংক্রিয়ভাবে নতুন ভার্সন ডিপ্লয় করে দেবে (১-২ মিনিট)
3. কোনো টার্মিনাল/কমান্ড লাগবে না

## প্রজেক্ট স্ট্রাকচার

```
src/
  App.jsx              — রাউটার রুট ("/" → MainApp, "/admin" → AdminPage)
  MainApp.jsx           — মূল অ্যাপ শেল: auth, ডেটা লোড, কুইজ সেশন লজিক, ও
                          registry থেকে জেনেরিক্যালি ফিচার রেন্ডার করা
                          (নতুন ফিচার যোগ/মুছলে এই ফাইল বদলাতে হয় না)
  lib/
    AppContext.jsx       — shared context; useApp() দিয়ে যেকোনো ফিচার
                          কম্পোনেন্ট নিজে থেকেই user/subjects/topics/exams ও
                          কুইজ-শুরু-করার ফাংশনগুলো নিয়ে নেয় (কোনো prop লাগে না)
    supabaseClient.js
    utils.js
  features/
    registry.js           — ⭐ Student পোর্টালের সব ফিচারের তালিকা — ফিচার
                          যোগ/মুছা/রিঅর্ডার করতে শুধু এই ফাইলটাই এডিট করবেন
    adminRegistry.js       — একই প্যাটার্নে Admin Panel-এর ট্যাবের তালিকা
  pages/
    AdminPage.jsx        — /admin রুট: নিজস্ব লগইন-চেক ও admin-গার্ড
  components/
    Login.jsx           — Google লগইন স্ক্রিন
    TopBar.jsx           — নেভিগেশন বার (registry.js থেকে automatically বানে)
    Dashboard.jsx        — ব্যক্তিগত প্রোগ্রেস + streak/level/badges (শুধু নিজের ডেটা)
    PracticeSetup.jsx    — সব বিষয়/একটা বিষয়/একটা টপিক বেছে নেওয়া
    Quiz.jsx             — OMR বাবল-শিট স্টাইলে প্রশ্ন-উত্তর (system view)
    Result.jsx           — সেশন শেষে ফলাফল, celebration + XP (system view)
    SessionReview.jsx     — ভুল ও বাদ পড়া প্রশ্ন রিভিউ (system view)
    ExamsList.jsx        — 📁 পরীক্ষা আর্কাইভ
    SmartSearch.jsx       — 🔍 কুইক সার্চ (typo-tolerant) + ফেভারিট টগল
    Favorites.jsx         — ⭐ বুকমার্ক করা প্রশ্নের রিভিশন তালিকা
    TopicGuru.jsx         — 🧭 টপিক ট্রি — নেস্টেড সাব-টপিক, স্টাডি মোড, প্রোগ্রেস
    QuizBuilder.jsx       — 🎛️ কাস্টম কুইজ (উৎস/বিষয়/সংখ্যা/সময়সীমা)
    LiveExam.jsx          — 🔴 লাইভ পরীক্ষা + র‍্যাংকিং
    Admin.jsx            — Admin Panel শেল (adminRegistry.js থেকে ট্যাব রেন্ডার করে)
    admin/
      SubjectsTab.jsx, TopicsTab.jsx, ExamsTab.jsx,
      QuestionsTab.jsx, LiveExamsTab.jsx   — প্রতিটা Admin ট্যাব আলাদা, স্বনির্ভর ফাইল

schema.sql              — ডাটাবেজ স্কিমা + RLS প্রাইভেসি নিয়ম (সব migration-সহ, সম্পূর্ণ)
reset_database.sql      — শুধু এই অ্যাপের টেবিল/ফাংশন মুছে ক্লিন স্লেট বানায় (schema.sql আবার চালানোর আগে)
admin_migration.sql      — is_admin কলাম ও Admin-only write নিয়ম (schema.sql-এর পরে চালান)
questions_upgrade_migration.sql — MCQ+Short Answer সাপোর্ট, difficulty বাদ (বিদ্যমান DB থাকলে চালান)
questions_list_migration.sql — created_at কলাম, Admin Panel-এ প্রশ্ন তালিকা দেখানোর জন্য
exam_questions_migration.sql — subject_id ঐচ্ছিক করার নিশ্চয়তা (বেশিরভাগ ক্ষেত্রে এমনিতেই প্রযোজ্য না)
smart_search_migration.sql — pg_trgm fuzzy search + favorites টেবিল (বিদ্যমান DB থাকলে চালান)
topic_guru_migration.sql — নেস্টেড সাব-টপিক + topic_reads প্রোগ্রেস টেবিল (বিদ্যমান DB থাকলে চালান)
quiz_builder_migration.sql — practice_sessions-এ 'custom' মোড অনুমোদন
live_exam_migration.sql — লাইভ পরীক্ষা টেবিল + anonymized র‍্যাংকিং ফাংশন
live_exam_names_migration.sql — লিডারবোর্ডে নাম দেখানোর জন্য ফাংশন আপডেট (live_exam_migration.sql-এর পরে চালান)
vercel.json              — SPA rewrite (যাতে সরাসরি /admin লিংক কাজ করে)
```
